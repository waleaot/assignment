
 var numba = Math.ceil(Math.random() * 10);
 var usernum = prompt('Guess the number between 1 and 10 ');
 if (usernum == numba)
   alert('the number Matched');
  else
   alert('number didn't match');